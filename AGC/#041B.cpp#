#include <bits/stdc++.h>
typedef long long ll;
using namespace std;

int main(){
  int N, M, V, P;
  cin >> N >> M >> V >> P;
  vector<int> A(N);
  for(int i = 0; i < N; i++){
    cin >> A[i];
  }
  sort(A.begin(), A.end());
  int std = A[N-P];
  
}
