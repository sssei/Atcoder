#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
#define INF 1000000000

struct st{
  ll num;
  int eat[3003];
};

int main(){
  int N,T;
  cin >> N >> T;
  int a[N][2];
  for(int i = 0; i < N; i++){
    cin >> a[i][0] >> a[i][1];
  }
  st dp[T+3];
  for(int i = 0; i < T+3; i++){
    dp[i].num = INF;
  }
  for(int i = 0; i < T+3; i++){
    dp[]
  }
}
