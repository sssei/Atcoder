#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <utility>
#include <cmath>
typedef long long ll;
using namespace std;

int main(){
  int N,M;
  cin >> N >> M;
  vector<int> P(M), Y(M);
  for(int i = 0; i < M; ++i){
    cin >> P[i] >> Y[i];
  }
  
}
